# Ops 10/10 — deploy excludes, SoT honesty, dead twins, CinePro docs

**Repo:** `/Users/husnainali/cinehome` (GitHub SoT). Did not touch `cinehome-sot`.
**Date:** 2026-08-14
**Scope:** ops/docs only. No player, scraper providers, views (except deleted unused twins). No env flips.

## 1. `scripts/deploy.sh` rsync `--delete` excludes

`--delete` previously preserved `.git`, `node_modules`, `.next`, `db/`, `.env*`, but **not** the remux cache or QA cookies. That is the Mac-deploy failure mode: rsync deleted Docker-owned `transcode-cache/` on the server.

Added (first-match rsync excludes, with `db/`):

- `--exclude 'transcode-cache/'` — bind-mounted remux 4K cache
- `--exclude '.browser-qa/'` — Playwright storage state
- `--exclude '.runtime-cache/'` — already in `.dockerignore`; keep off the wire / `--delete`

`scripts/deploy-safety.test.ts` now asserts those three exclude strings plus `rsync -az --delete`.

```
bun test scripts/deploy-safety.test.ts
3 pass / 0 fail
```

## 2. SoT docs tell the truth

| File | Change |
|------|--------|
| `AGENTS.md` | Local SoT is `/Users/husnainali/cinehome`. Canonical remote: GitHub `nsfwhusnain-coder/cinehome`. `cinehome-sot` is STALE — do not edit or deploy from it. Workflow cwd updated. |
| `CINEHOME.md` | Added `/Users/husnainali/cinehome-sot` (stale un-remoted Mac mirror) to the must-not-deploy-from list. GitHub remains canonical. |
| `.grok/skills/cinehome-dev/SKILL.md` | cwd `/Users/husnainali/cinehome` |
| `.grok/skills/cinehome-deploy/SKILL.md` | `cd /Users/husnainali/cinehome` |

## 3. Deleted unused twins

Import audit first (nothing imported the roots):

- Pages: `@/views/watch` (`src/app/watch/[type]/[id]/page.tsx`)
- Store: `@/stores/player-store` (player, dock, controls, skip-intro, playback helpers)

Deleted:

- `src/watch.tsx`
- `src/player-store.ts`

Canonical copies remain: `src/views/watch.tsx`, `src/stores/player-store.ts`.

## 4. CinePro comment honesty (docs only)

Code (`mini-services/stream-scraper/providers/circuit.ts`): enabled when `CINEPRO_URL` is set, unless `PROVIDER_CINEPRO=0`. Also on via `PROVIDER_CINEPRO=1` or `CINEPRO_EVAL_UNTIL`.

Compose (`docker-compose.yml` L45) injects:

`CINEPRO_URL=${CINEPRO_URL:-http://cinepro-core:3000}`

So live is **on** without `PROVIDER_CINEPRO=1`. `CINEHOME.md` no longer claims fail-closed / eval-only. Live 2026-08-14: cinepro enabled, often `cinepro_timeout_8000` (8s fast budget). **Did not touch production env.**

## Leftover (out of requested scope)

`.grok/skills/cinehome-browser/SKILL.md` still says `cd /Users/husnainali/cinehome-sot` and screenshots under that tree. Not in the file list.

Historic `docs/*` (AGENT-SETUP, research notes) still mention `cinehome-sot`. Not rewritten.

## Not done (by contract)

- No deploy
- No `TRANSCODER_ENABLED`
- No port 3030 publish
- No `.env` / secrets
- No player / scraper / views product edits
