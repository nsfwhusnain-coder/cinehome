# UI 10/10 handoff

Repo: `/Users/husnainali/cinehome`  
Did not edit `video-player.tsx` or `mini-services/stream-scraper/**`.  
Did not undo Hisense type/hex/font rules in `globals.css`.

`bunx tsc --noEmit` has no errors under `src/` (pre-existing scraper test fetch casts still fail outside this work).  
`bun test src/lib/profile-preferences.test.ts src/views/login-callback.test.ts` — 9 pass.

## Files changed

### Login + chrome
- `src/views/login.tsx` — safe `callbackUrl`, session-expired banner
- `src/views/login-callback.test.ts` — callback sanitizer
- `src/app/(main)/login/page.tsx` — passes `searchParams`
- `src/app/(main)/layout.tsx` + `src/app/(main)/main-chrome.tsx` — hide dock/footer/ambient/padding on `/login`
- `src/components/mobile-dock.tsx` — `/login` guard

### Continue + cards
- `src/views/continue-watching.tsx` — real `<Link>`, sibling remove, white progress
- `src/views/home.tsx` — Continue card is a link; overflow sibling; adult filter
- `src/components/card-overflow-menu.tsx` — always-visible 48px on TV
- `src/components/movie-card.tsx` — poster hover/focus white Play on scrim; `adult?`

### Hubs / person / TV
- `src/views/browse-hub.tsx` — `viewAllHref={`/browse/${row.id}`}` + adult filter
- `src/views/browse-category.tsx` — adult filter on View-all grids
- `src/lib/tmdb.ts` — `personDetails`, `personCredits`, `withoutAdultTitles`, `include_adult: false` on discover
- `src/app/api/tmdb/[...path]/route.ts` — `person/:id`, `person/:id/combined_credits`
- `src/app/(main)/person/[id]/page.tsx` + `loading.tsx`
- `src/views/person.tsx`
- `src/components/person-card.tsx` — in-app `/person/:id`
- `src/components/empty-states.tsx` — `PersonError`
- `src/components/hero-carousel.tsx` — `data-tv-safe`, `data-tv-first-focus`, no Ken Burns on TV
- `src/components/tv-spatial-navigation.tsx` — skip `/login`; prefer `[data-tv-first-focus]`
- `src/app/globals.css` — TV Ken Burns kill + overflow always visible

### Watch / settings / household
- `src/views/watch.tsx` — removed resume toast (player notice stays)
- `src/views/settings.tsx` — Auto vs Ultra copy; Hide adult toggle
- `src/views/search.tsx` — adult filter
- `src/lib/profile-preferences.ts` + `.server.ts` — `hide_adult` KV
- `src/lib/profile-preferences.test.ts`
- `src/app/api/preferences/route.ts` — GET `hideAdult`; PATCH `{ hideAdult }` only
- `src/hooks/use-hide-adult.ts`

## QA

1. **callbackUrl + session expired**  
   Sign out mid-session (or hit `/login?error=SessionExpired&callbackUrl=/movie/550`). Banner: “Your session expired. Sign in again.” After PIN, land on `/movie/550`. Reject `/login?callbackUrl=//evil` (stays `/`).

2. **Login chrome**  
   `/login` has no mobile dock, no footer, no extra `pb-20`. Navbar still hidden until signed in. Name field still auto-focuses; D-pad must not jump to a lettermark.

3. **Continue Watching 10-foot**  
   `/continue`: each title is a link (tab / TV OK). Remove is a sibling, not nested. Progress bar is white. Home Continue Watching matches (link + sibling `...`).

4. **Overflow vs TV focus**  
   `?tv=1`: `...` is visible (~48px) and in the spatial graph. Pointer: still hover-reveal.

5. **Card Play (KD20)**  
   Poster hover/focus: dark scrim + centered white circular Play. `...` stays top-right. One tab stop per card. Backdrop/continue keep bottom-info (no circular play).

6. **Hub View all**  
   `/movies` and `/shows` rails show View all → `/browse/{row.id}` (including `genre-movie-28`, `trending-movies`). Trending grids may hide load-more (`paged: false`); grid still opens.

7. **Person page**  
   Cast card or search person → `/person/{id}` same tab. Photo, bio, Movies + Shows `MovieCard` rails, optional white Play on a featured credit. 404/error uses `PersonError`. `/api/tmdb/person/:id` and `.../combined_credits` require auth.

8. **TV polish**  
   `?tv=1` home: static hero (no Ken Burns). Play + dots sit inside `[data-tv-safe]`. First focus is hero Play, not the lettermark. Login PIN/name focus unbroken.

9. **One resume toast**  
   Resume a title: only the in-player notice. No Sonner “Resumed from …”.

10. **Settings copy**  
    Playback quality: Auto = start 1080, climb to 4K if line + 4K source. Ultra = prefer 4K in the roster; still fast-starts HD unless Maximum. No AllDebrid.

11. **Hide adult**  
    Settings → Household → “Hide adult titles” default ON (`UserSetting` `hide_adult`). Filters `adult === true` on home, hubs, search, View-all, person filmography.

Browser QA (after deploy):  
`bun scripts/browser/qa.ts screenshot home`  
`bun scripts/browser/qa.ts screenshot login`  
`bun scripts/browser/qa.ts open /person/500` (Tom Cruise)  
`bun scripts/browser/qa.ts ui-check`
